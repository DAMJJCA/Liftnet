spring:
  datasource:
    url: jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.dixynkwbhduwjerffqsg
    password: LiftnetJorgeGon

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

# JWT (CONFIGURABLE)
jwt:
  secret: liftnet_super_secure_secret_key_change_in_prod
  expiration: 3600000 # 1 hora (ms)