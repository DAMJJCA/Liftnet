spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require}
    username: ${DB_USER:postgres.dixynkwbhduwjerffqsg}
    password: ${DB_PASSWORD:LiftnetJorgeGon}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:

  secret: ${JWT_SECRET:liftnet_jwt_secret_key_256_bits_min_2026!!}
  expiration: ${JWT_EXPIRATION:3600000}